# DecodeLabs_Project-3
Project 3 uses unsupervised learning to segment retail consumers into strategic personas. The end-to-end pipeline standardizes synthetic behavioral data using StandardScaler, reduces dimensionality via PCA to retain 97.4% variance, applies K-Means clustering guided by the silhouette score ($K=2$), and visualizes a 5-panel dashboard.
